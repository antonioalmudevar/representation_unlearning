model:
  name: "resnet34_tinyimagenet"
  num_classes: 200

dataset:
  name: "tiny_imagenet"
  data_root: "/home/voz/shared/database/vision/tiny-imagenet-200"
  batch_size: 256
  num_workers: 8
  has_val: false
  split_protocol:
    type: "random_forget"
    forget_ratio: 0.10

deterministic: true

method:
  name: retrain
  epochs: 160
  lr: 0.001
  weight_decay: 0.0005
  max_norm: 0.0
  
  optimizer: adamw
  optimizer_cfg:
    betas: [0.9, 0.999]
  
  scheduler: cosine
  scheduler_cfg:
    warmup_epochs: 0