from setuptools import setup

setup(  name='representation_unlearning',
        version='1.0',
        description='Representation Unlearning: Forgetting through Information Compression',
        packages=['src'],
    )